<h2>New description for <%= @procedure.name %></h2>

<%= render "form.html", changeset: @changeset,
                        procedure_id: @procedure.id,
                        action: old_procedure_description_path(@conn, :create) %>

<%= link "Jump to procedure", to: old_procedure_path(@conn, :show, @procedure.id), class: "btn btn-default" %>
